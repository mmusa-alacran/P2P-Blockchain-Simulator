# Host ports
NODE1_PORT=5001
NODE2_PORT=5002

# Public URLs used by the app to advertise itself (for ngrok or other tunnels).
# Leave as localhost for local-only demos, or set to the tunnel URL(s).
NODE1_PUBLIC_URL=http://localhost:5001
NODE2_PUBLIC_URL=http://localhost:5002

# Proof-of-Work difficulty
DIFFICULTY=3

